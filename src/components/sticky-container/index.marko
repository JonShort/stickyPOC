style {
    .container {
        background: hotpink;
        display: block;
        width: 100%;
    }

    .sticky {
        position: sticky;
        top: 0;
    }
}

div.container style="height: ${input.height}"
  div.sticky -- ${input.text}